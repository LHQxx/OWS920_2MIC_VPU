#ifndef __SBOX_CONNECT_EMITTER_H__
#define __SBOX_CONNECT_EMITTER_H__

#include "typedef.h"

void emitter_last_phone_mac_init(void);
void custom_ctrle_emitter_info(u8 *data, u16 len);
void custom_ctrle_edr_conn(u8 cmd);

#endif
